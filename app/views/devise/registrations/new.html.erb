<div class="container mt-5 mb-5">

  <h2 class="row">サインアップ</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <div class="row">
      <%= render "devise/shared/error_messages", resource: resource %>
    </div>

    <div class="field row mt-5 text-center">
      <div class="col"><%= f.label :名前 %></div>
      <div class="col"><%= f.text_field :name, autofocus: true %></div>
    </div>

    <div class="field row mt-3 text-center">
      <div class="col"><%= f.label :メールアドレス %></div>
      <div class="col"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>

    <div class="field row mt-3 text-center">
      <div class="col"><%= f.label :パスワード %></div>
      <div class="col"><%= f.password_field :password, autocomplete: "new-password" %></div>
    </div>

    <div class="field row mt-3 text-center">
      <div class="col"><%= f.label :パスワード（確認用） %></div>
      <div class="col"><%= f.password_field :password_confirmation, autocomplete: "new-password" %></div>
    </div>

    <div class="col text-center mt-5">
        <%= f.submit "サインアップ", class:"btn btn-success" %>
    </div>

  <% end %>

  <div class="row mt-5">
    既に登録済みの方は<%= link_to "こちら", new_user_session_path %>からログインしてください。
  </div>

</div>